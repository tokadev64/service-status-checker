import type { PresentationTone } from '../shared/types.ts';

export const toneIcons: Record<PresentationTone, string> = {
  green: 'check_circle',
  yellow: 'warning',
  red: 'error',
  gray: 'help',
};
